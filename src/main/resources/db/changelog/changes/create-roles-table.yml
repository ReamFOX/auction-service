databaseChangeLog:
  - changeSet:
      id: create-roles-table
      author: Oleksandr Farion
      changes:
        - createTable:
            tableName: roles
            columns:
              - column:
                  name: id
                  type: bigint
                  autoIncrement: true
                  constraints:
                    primaryKey: true
              - column:
                  name: name
                  type: enum('BUYER', 'SELLER', 'ADMIN')
                  constraints:
                    nullable: false
                    unique: true
